<Application x:Class="WpfApp1.App"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="clr-namespace:WpfApp1"
			 StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TextBoxes.xaml"/>
                <ResourceDictionary Source="/Imgs.xaml"/>
                <ResourceDictionary Source="/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Color x:Key="FirstColor" R="55" G="97" B="105" A="255"/>
            <Color x:Key="SecondColor" R="102" G="187" B="204" A="255" />
            <Color x:Key="TherdColor" R="121" G="196" B="210" A="255" />
            <Color x:Key="BorderColor" R="153" G="209" B="255" A="255" />
            <Style x:Key="PanelButtons">
                <Setter Property="FrameworkElement.Margin" Value="5 0 5 0"/>
                <Setter Property="Control.Background" Value="#6fBBCC"/>
            </Style>
            <Style TargetType="{x:Type CheckBox}">
                <Setter Property="Margin" Value="10 0 0 0"/>
            </Style>
            <Style x:Key="Smaller">
                <Setter Property="FrameworkElement.Margin" Value="0 1 0 0"/>
                <Setter Property="Control.FontSize" Value="10"/>
            </Style>
            <Style TargetType="{x:Type TextBox}" >
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="Margin" Value="0 0 0 5"/>
                <Setter Property="MinWidth" Value="30"/>
            </Style>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Margin" Value="5 0 5 0"/>
            </Style>
            <Style TargetType="{x:Type Label}">
                <Setter Property="Margin" Value="0 0 0 5"/>
            </Style>
            <Style TargetType="{x:Type Button}" />
            <Style TargetType="{x:Type ComboBox}" />
            <Style TargetType="{x:Type TabControl}" />
            <Style TargetType="{x:Type TabItem}" />
            <Style TargetType="{x:Type Grid}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush>
                            <GradientStop Color="{StaticResource FirstColor }"/>
                            <GradientStop Color="{StaticResource SecondColor }" Offset="1"/>
                            <GradientStop Color="{StaticResource TherdColor }" Offset="0.821"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
